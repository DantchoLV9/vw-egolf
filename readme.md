# Simple page built in HTML, Tailwind and Javascript

*Note: Run the following command when developing in order to build the output.css in real time.*
`npx tailwindcss -i ./css/input.css -o ./css/output.css --watch`